{% extends 'public/base.html.twig' %}

{% block title %}Firmalar{% if search %} - "{{ search }}" araması{% endif %} | Makine Ticaret{% endblock %}
{% block description %}Güvenilir firma rehberi. Sanayi sektöründe faaliyet gösteren firmalar ve iletişim bilgileri.{% endblock %}

{% block body %}
<!-- Page Header -->
<section class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between">
            <div class="flex-1">
                <nav class="flex mb-4" aria-label="Breadcrumb">
                    <ol class="flex items-center space-x-2 text-sm text-gray-500">
                        <li>
                            <a href="{{ path('public_home') }}" class="hover:text-primary-600">Ana Sayfa</a>
                        </li>
                        <li class="flex items-center">
                            <svg class="w-4 h-4 mx-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                            </svg>
                            <span class="text-gray-900">Firmalar</span>
                        </li>
                    </ol>
                </nav>
                
                <h1 class="text-3xl font-bold text-gray-900 mb-2">
                    {% if search %}
                        "{{ search }}" için Firma Arama Sonuçları
                    {% else %}
                        Tüm Firmalar
                    {% endif %}
                </h1>
                
                <p class="text-gray-600">
                    {% if totalCompanies > 0 %}
                        {{ totalCompanies }} firma bulundu
                    {% else %}
                        Firma bulunamadı
                    {% endif %}
                </p>
            </div>
            
            <!-- Search Form -->
            <div class="mt-6 lg:mt-0 lg:ml-8">
                <form method="GET" class="flex flex-col sm:flex-row gap-3">
                    <div class="relative flex-1 sm:min-w-80">
                        <input type="text" 
                               name="search" 
                               value="{{ search }}" 
                               placeholder="Firma adı, faaliyet alanı ara..." 
                               class="w-full pl-10 pr-4 py-2.5 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500">
                        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
                            <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                            </svg>
                        </div>
                    </div>
                    <button type="submit" class="px-6 py-2.5 bg-primary-600 text-white font-medium rounded-lg hover:bg-primary-700 transition-colors">
                        Ara
                    </button>
                </form>
            </div>
        </div>
    </div>
</section>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    {% if companies|length > 0 %}
        <!-- Companies Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
            {% for company in companies %}
                <div class="bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden hover:shadow-lg transition-all duration-300 group">
                    <!-- Company Header -->
                    <div class="p-6">
                        <div class="flex items-center mb-4">
                            {% if company.logoPath %}
                                <img src="{{ asset(company.logoPath) }}" 
                                     alt="{{ company.name }}" 
                                     class="w-16 h-16 rounded-lg object-cover mr-4 group-hover:scale-105 transition-transform">
                            {% else %}
                                <div class="w-16 h-16 bg-gradient-to-r from-primary-500 to-primary-600 rounded-lg flex items-center justify-center mr-4 group-hover:scale-105 transition-transform">
                                    <span class="text-2xl font-bold text-white">
                                        {{ company.name|slice(0,1)|upper }}
                                    </span>
                                </div>
                            {% endif %}
                            <div class="flex-1">
                                <h3 class="text-xl font-semibold text-gray-900 group-hover:text-primary-600 transition-colors">
                                    <a href="{{ path('public_company_detail', {id: company.id}) }}">
                                        {{ company.name }}
                                    </a>
                                </h3>
                                {% if company.shortName and company.shortName != company.name %}
                                    <p class="text-gray-500 text-sm">{{ company.shortName }}</p>
                                {% endif %}
                            </div>
                        </div>
                        
                        <!-- Company Info -->
                        {% if company.activityType %}
                            <div class="mb-3">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-primary-100 text-primary-800">
                                    {{ company.activityType }}
                                </span>
                            </div>
                        {% endif %}
                        
                        {% if company.about %}
                            <p class="text-gray-600 text-sm mb-4 line-clamp-3">
                                {{ company.about|slice(0, 150) }}{% if company.about|length > 150 %}...{% endif %}
                            </p>
                        {% endif %}
                        
                        <!-- Company Details -->
                        <div class="space-y-2 text-sm text-gray-600 mb-4">
                            {% if company.foundedYear %}
                                <div class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                                    </svg>
                                    <span class="font-medium">Kuruluş:</span> {{ company.foundedYear }}
                                </div>
                            {% endif %}
                            
                            {% if company.phone %}
                                <div class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
                                    </svg>
                                    <a href="tel:{{ company.phone }}" class="text-primary-600 hover:text-primary-700">
                                        {{ company.phone }}
                                    </a>
                                </div>
                            {% endif %}
                            
                            {% if company.email %}
                                <div class="flex items-center">
                                    <svg class="w-4 h-4 mr-2 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                                    </svg>
                                    <a href="mailto:{{ company.email }}" class="text-primary-600 hover:text-primary-700">
                                        {{ company.email }}
                                    </a>
                                </div>
                            {% endif %}
                        </div>
                        
                        <!-- Action Button -->
                        <a href="{{ path('public_company_detail', {id: company.id}) }}" 
                           class="inline-flex items-center justify-center w-full px-4 py-2 bg-primary-600 text-white text-sm font-medium rounded-lg hover:bg-primary-700 transition-colors group-hover:bg-primary-700">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                            </svg>
                            Firmayı İncele
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div>
        
        <!-- Pagination -->
        {% if totalPages > 1 %}
            <div class="flex items-center justify-between border-t border-gray-200 bg-white px-4 py-3 sm:px-6 rounded-lg">
                <div class="flex flex-1 justify-between sm:hidden">
                    {% if currentPage > 1 %}
                        <a href="{{ path('public_companies', app.request.query.all|merge({page: currentPage - 1})) }}" 
                           class="relative inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">
                            Önceki
                        </a>
                    {% endif %}
                    {% if currentPage < totalPages %}
                        <a href="{{ path('public_companies', app.request.query.all|merge({page: currentPage + 1})) }}" 
                           class="relative ml-3 inline-flex items-center rounded-md border border-gray-300 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">
                            Sonraki
                        </a>
                    {% endif %}
                </div>
                <div class="hidden sm:flex sm:flex-1 sm:items-center sm:justify-between">
                    <div>
                        <p class="text-sm text-gray-700">
                            <span class="font-medium">{{ ((currentPage - 1) * 12) + 1 }}</span>
                            -
                            <span class="font-medium">{{ (currentPage * 12) > totalCompanies ? totalCompanies : (currentPage * 12) }}</span>
                            arası,
                            <span class="font-medium">{{ totalCompanies }}</span>
                            sonuçtan
                        </p>
                    </div>
                    <div>
                        <nav class="isolate inline-flex -space-x-px rounded-md shadow-sm" aria-label="Pagination">
                            <!-- Previous Page -->
                            {% if currentPage > 1 %}
                                <a href="{{ path('public_companies', app.request.query.all|merge({page: currentPage - 1})) }}" 
                                   class="relative inline-flex items-center rounded-l-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                    <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                        <path fill-rule="evenodd" d="M12.79 5.23a.75.75 0 01-.02 1.06L8.832 10l3.938 3.71a.75.75 0 11-1.04 1.08l-4.5-4.25a.75.75 0 010-1.08l4.5-4.25a.75.75 0 011.06.02z" clip-rule="evenodd" />
                                    </svg>
                                </a>
                            {% endif %}
                            
                            <!-- Page Numbers -->
                            {% for page in 1..totalPages %}
                                {% if page == currentPage %}
                                    <span class="relative z-10 inline-flex items-center bg-primary-600 px-4 py-2 text-sm font-semibold text-white focus:z-20 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary-600">
                                        {{ page }}
                                    </span>
                                {% elseif page == 1 or page == totalPages or (page >= currentPage - 2 and page <= currentPage + 2) %}
                                    <a href="{{ path('public_companies', app.request.query.all|merge({page: page})) }}" 
                                       class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-900 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                        {{ page }}
                                    </a>
                                {% elseif page == currentPage - 3 or page == currentPage + 3 %}
                                    <span class="relative inline-flex items-center px-4 py-2 text-sm font-semibold text-gray-700 ring-1 ring-inset ring-gray-300 focus:outline-offset-0">
                                        ...
                                    </span>
                                {% endif %}
                            {% endfor %}
                            
                            <!-- Next Page -->
                            {% if currentPage < totalPages %}
                                <a href="{{ path('public_companies', app.request.query.all|merge({page: currentPage + 1})) }}" 
                                   class="relative inline-flex items-center rounded-r-md px-2 py-2 text-gray-400 ring-1 ring-inset ring-gray-300 hover:bg-gray-50 focus:z-20 focus:outline-offset-0">
                                    <svg class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                                        <path fill-rule="evenodd" d="M7.21 14.77a.75.75 0 01.02-1.06L11.168 10 7.23 6.29a.75.75 0 111.04-1.08l4.5 4.25a.75.75 0 010 1.08l-4.5 4.25a.75.75 0 01-1.06-.02z" clip-rule="evenodd" />
                                    </svg>
                                </a>
                            {% endif %}
                        </nav>
                    </div>
                </div>
            </div>
        {% endif %}
        
    {% else %}
        <!-- No Companies Found -->
        <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-12 text-center">
            <svg class="mx-auto h-16 w-16 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
            </svg>
            <h3 class="text-lg font-medium text-gray-900 mb-2">Firma Bulunamadı</h3>
            <p class="text-gray-600 mb-6">
                {% if search %}
                    "{{ search }}" aramanızla eşleşen firma bulunamadı.
                {% else %}
                    Henüz hiç firma eklenmemiş.
                {% endif %}
            </p>
            <div class="flex flex-col sm:flex-row gap-3 justify-center">
                <a href="{{ path('public_companies') }}" 
                   class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 bg-white hover:bg-gray-50">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"></path>
                    </svg>
                    Tüm Firmalar
                </a>
                <a href="{{ path('public_home') }}" 
                   class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-primary-600 hover:bg-primary-700">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
                    </svg>
                    Ana Sayfa
                </a>
            </div>
        </div>
    {% endif %}
</div>
{% endblock %}
